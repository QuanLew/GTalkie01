### To run frontend + voiceRegconize
in root, execute:
`npm install`
`npx expo prebuild -p ios`
`npm run ios`

### To run frontend
in root, execute:
`npm install`
`npm start`
then choose `i`

### To run backend
navigate to backend
execute
`npm run dev`

